apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: stream-sampleservice
  namespace: default
spec:
  template:
    spec:
      
      
      
      containers:
      - image: github.com/mattmoor/korpc-sample/gen/entrypoint/sampleservice/stream
        ports:
        - name: h2c
          containerPort: 8080
        readinessProbe:
          exec:
            command: ["/ko-app/stream", "probe"]
        livenessProbe:
          exec:
            command: ["/ko-app/stream", "probe"]
        env:
        resources:
          limits:
          requests:
