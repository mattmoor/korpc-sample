apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: streamin-sampleservice
  namespace: default
spec:
  template:
    spec:
      
      
      
      containers:
      - image: github.com/mattmoor/korpc-sample/gen/entrypoint/sampleservice/streamin
        ports:
        - name: h2c
          containerPort: 8080
        readinessProbe:
          exec:
            command: ["/ko-app/streamin", "probe"]
        livenessProbe:
          exec:
            command: ["/ko-app/streamin", "probe"]
        env:
        resources:
          limits:
          requests:
