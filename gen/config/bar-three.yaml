apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: bar-three
  namespace: default
spec:
  template:
    spec:
      
      
      
      containers:
      - image: github.com/mattmoor/korpc-sample/gen/entrypoint/three/bar
        ports:
        - name: h2c
          containerPort: 8080
        readinessProbe:
          exec:
            command: ["/ko-app/bar", "probe"]
        livenessProbe:
          exec:
            command: ["/ko-app/bar", "probe"]
        env:
        resources:
          limits:
          requests:
