apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: blur-two
  namespace: default
spec:
  template:
    spec:
      
      
      
      containers:
      - image: github.com/mattmoor/korpc-sample/gen/entrypoint/two/blur
        ports:
        - name: h2c
          containerPort: 8080
        readinessProbe:
          exec:
            command: ["/ko-app/blur", "probe"]
        livenessProbe:
          exec:
            command: ["/ko-app/blur", "probe"]
        env:
        resources:
          limits:
          requests:
